builds:
- env:
    - CGO_ENABLED=1
  goos:
    - darwin
  goarch:
    - amd64
    - arm64
    - '386'
  flags:
    - -trimpath
  ignore:
  binary: '{{ .ProjectName }}_v{{ .Version }}'

archives:
  - format: zip
    name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}'

checksum:
  extra_files:
    - glob: 'terraform-registry-manifest.json'
      name_template: '{{ .ProjectName }}_{{ .Version }}_manifest.json'
  name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Runtime.Goos }}_SHA256SUMS'
  algorithm: sha256

release:
  github:
    owner: explorer2036
    name: terraform-provider-snowflake
  extra_files:
    - glob: 'terraform-registry-manifest.json'
      name_template: '{{ .ProjectName }}_{{ .Version }}_{{ .Runtime.Goos }}_manifest.json'
